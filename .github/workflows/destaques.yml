name: Destaques

on:
  push:
    branches:
      - main
  schedule:
    - cron: 0 * * * *

permissions:
  contents: read
  id-token: write

jobs:
  destaques:
    uses: ./.github/workflows/etl.yml
    with:
      endpoint: mercado/destaques
      filter: .
      table: destaques
